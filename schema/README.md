# Cube.dev Schema

Esta carpeta es la fuente de verdad para medidas y dimensiones.

- Coloca aqui tus definiciones de cubos de Cube.dev (`.js` o `.ts`).
- El crawler de metadata lee estos archivos para indexar Pinecone.
- El pipeline de IA usa esa indexacion para generar queries `/load`.
